/* const form = document.forms.form_cadastro; */
import { pesquisaCep } from "../js/pesquisa-cep";
/* const checkPassword = (password, password2) => {
	return password === password2;
}; */
pesquisaCep();

/* form.addEventListener("submit", (e) => {
	e.preventDefault();
	const { senha, confirm_senha } = form;
	const result = checkPassword(senha, confirm_senha);
	if (result) {
		console.log("Senha iguais");
	} else {
		const { nome, email, cep, uf, cidade } = form;
		const pessoa = {
			email: email.value,
			senha: senha.value,
			nome: nome.value,
		};
		console.log(pessoa);
	} */

/* const { nome, email, cep, uf, cidade, endereco, senha } = form; */
/* const pessoa = {
		email: email.value,
		senha: senha.value,
		senha: senha.value,
	};
	console.log(pessoa); */
/* 	email.value = " ";
	senha.value = " "; */
//});

/* buttonClick.addEventListener("click", () => {
	buttonClick.preventDefault();
	const form = document.forms.form_cadastro;
	console.log("Juliete");
	console.log(form);
}); */
